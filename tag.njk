---
pagination:
  data: gameTags
  size: 1
  alias: tag
permalink: "{{ tag | tagUrl }}"
layout: layout
---
{% for game in games %}
    {% if game | contains(tag, 'tags') %}
        <a href="{{ game | gameUrl }}">{{ game.name }}</a>
    {% endif %}
{% endfor %}