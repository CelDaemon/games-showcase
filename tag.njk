---
pagination:
  data: gameTags
  size: 1
  alias: tag
  addAllPagesToCollections: true
permalink: "{{ tag | tagUrl }}"
layout: layout
tags: page
date: 'git Last Modified'
---
{% for game in games %}
    {% if game | contains(tag, 'tags') %}
        <a href="{{ game | gameUrl }}">{{ game.name }}</a>
    {% endif %}
{% endfor %}